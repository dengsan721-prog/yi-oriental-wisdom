import { buildTraditionalReadings } from "../../lib/yi/traditions";
import type { FourPillarsResult } from "../../lib/yi/types";
export function TraditionSection({ chart }: { chart: FourPillarsResult }) { const readings = buildTraditionalReadings(chart); return <section className="report-section"><header><small>传统技法</small><h1>辅助传统，逐层与主盘校验</h1><p>三种方法各有七层；与子平主盘冲突时，以主盘与现实证据为先。</p></header><div className="detail-groups">{readings.map((reading, index) => <section key={reading.method}><h2>{reading.method}</h2><p>{reading.role}</p><details open={index === 0}><summary>核心三层 <small>展开</small></summary><div className="seven-layers">{reading.layers.slice(0, 3).map(layer => <p key={layer.title}><b>{layer.title}</b>{layer.observation}<small>{layer.mainChartComparison}</small></p>)}</div></details><details><summary>生活三层 <small>展开</small></summary><div className="seven-layers">{reading.layers.slice(3, 6).map(layer => <p key={layer.title}><b>{layer.title}</b>{layer.observation}<small>{layer.mainChartComparison}</small></p>)}</div></details><details><summary>边界与出处 <small>展开</small></summary><div className="seven-layers">{reading.layers.slice(6).map(layer => <p key={layer.title}><b>{layer.title}</b>{layer.observation}<small>{layer.mainChartComparison}<br />{layer.source}</small></p>)}<p><b>提醒</b>{reading.caution}</p></div></details></section>)}</div></section>; }
